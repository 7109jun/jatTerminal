# jat Terminal v6.0 실무 명령어 전체 가이드

이 문서는 jat Terminal에 실제로 구현되어 있는 모든 명령어를 다룬다. 

## 표기 규칙 (가장 먼저 틀리는 부분)

- 명령어는 `이름("문자열인자",숫자인자)` 형태의 괄호형이다. 문자열 인자는 반드시 큰따옴표로 감싼다. 숫자(ms, n 등)는 따옴표 없이 그대로 쓴다.
- `?명령어(...)` : Dry Run. 실제로 실행하지 않고 무엇을 할지만 보여준다. 물음표는 명령어 이름 바로 앞, 공백 없이 붙인다.
- `$명령어(...)` : Silent. `jat>` 에코 없이 조용히 실행한다. 매크로/배치 내부에서 로그를 줄일 때 쓴다.
- `?`와 `$`는 동시에 하나만 붙일 수 있는 게 아니라 둘 다 붙일 수도 있다(`?$`, `$?` 순서 무관). 단 실제로는 Dry Run이 무음보다 우선 표시된다.
- 대소문자는 명령어 이름 자체는 구분하지 않는다(`create`, `Create`, `CREATE` 모두 동작). 단 문자열 인자 내용(파일명 등)은 대소문자 그대로 처리된다.
- `Batch([...])`, `Macro("이름",[...])` 의 대괄호 안 명령어는 콤마로 구분한다. 이때 각 명령어 안에 콤마가 있는 문자열 인자가 있으면(`Create("a","1,2","txt")` 등) 괄호 깊이를 자동으로 세므로 문제없다. 콤마 개수를 직접 세서 걱정할 필요는 없다.
- `file find`, `file ext find`, `run script.jat` 은 괄호형이 아니라 공백으로 구분하는 명령어다. 괄호를 붙이면 동작하지 않는다.

---

## 1. 파일 생성/수정/이동

### Create("파일명","내용","확장자")
- 설명: 새 파일을 생성하고 내용을 즉시 기록한다.
- 예시: `Create("note","hello","txt")` → note.txt 생성
- 예시(확장자 생략, 파일명에 확장자 직접 포함): `Create("note.txt","hello")`
- 자주 틀리는 것: 인자 2개 미만이면 에러. 세 번째 인자(확장자) 없이 두 번째 자리에 확장자까지 넣은 이름을 써도 되는데, 이때 세 번째 인자를 빈 문자열로 남겨두면 안 되고 아예 생략해야 한다.

### Update("파일명","내용","확장자")
- 설명: Create와 인자 형태는 동일하지만, 대상 파일이 이미 존재해야만 동작한다.
- 예시: `Update("note.txt","new content")`
- 자주 틀리는 것: 파일이 없는 상태에서 Update를 쓰면 "파일이 존재하지 않습니다" 에러가 난다. 새로 만들 땐 반드시 Create.

### Copy("출발지","목적지")
- 설명: 파일 또는 폴더를 복사한다. 단일 파일은 진행률(%)이 표시된다.
- 예시: `Copy("data.csv","backup/data.csv")`
- 자주 틀리는 것: 목적지 폴더가 없으면 파일 복사는 실패할 수 있다. 폴더 자체를 복사할 땐 목적지에 새 폴더 이름까지 지정해야 한다(`Copy("src","dst")`는 dst라는 이름의 폴더가 생김).

### Move("출발지","목적지")
- 설명: 파일/폴더 이동. 같은 드라이브면 즉시 이동, 다른 드라이브/파일시스템이면 내부적으로 복사 후 원본 삭제로 대체된다.
- 예시: `Move("draft.md","archive/draft.md")`

### Rename("기존","새이름")
- 설명: 같은 폴더 안에서 이름만 바꾼다. 두 번째 인자는 경로가 아니라 새 파일명만 쓴다.
- 예시: `Rename("v1.txt","v2.txt")`
- 자주 틀리는 것: 두 번째 인자에 전체 경로를 넣으면 원래 폴더 기준으로 다시 합쳐지므로 의도와 다른 위치에 생길 수 있다. 폴더를 옮기고 싶으면 Move를 써야 한다.

### Duplicate("원본","복사본")
- 설명: Copy와 완전히 동일하게 동작한다. "백업 목적의 복사"라는 의도를 코드에 남기고 싶을 때 쓴다.
- 예시: `Duplicate("config.json","config.backup.json")`

### Open("파일/경로")
- 설명: OS 기본 프로그램으로 파일이나 경로를 연다(Windows는 ShellExecute, Linux는 xdg-open).
- 예시: `Open("report.pdf")`
- 자주 틀리는 것: Linux에서 xdg-open이 없는 최소 설치 환경이면 실패할 수 있다.

---

## 2. 삭제 (3단계 안전장치)

### Delete("파일명") - Lv.1
- 설명: 휴지통으로 이동. 실수해도 OS 휴지통에서 복구 가능.
- 예시: `Delete("temp.log")`
- 자주 틀리는 것: "완전 삭제"라고 착각하는 경우가 많다. Delete는 가장 약한 단계다.

### Remove("파일명") - Lv.2
- 설명: 실행 폴더 아래 `.jat_trash` 폴더로 격리 이동. 30일 지나면 자동 영구 삭제됨(다음 실행 시 정리됨).
- 예시: `Remove("old_backup")`
- 자주 틀리는 것: `.jat_trash` 폴더는 명령을 실행한 현재 작업 폴더 기준으로 생긴다. 다른 위치에 있는 줄 알고 못 찾는 경우가 흔하다. `Path()`로 현재 위치를 먼저 확인하는 습관을 들이는 게 좋다.

### Erase("파일명") - Lv.3
- 설명: 영구 삭제. 복구 불가능. 폴더면 안의 내용 전부 삭제.
- 예시: `Erase("build")`
- 자주 틀리는 것: 되돌릴 수 없으므로 실행 전 `?Erase("대상")`으로 먼저 확인하는 것이 정석이다. 특히 스크립트/매크로 안에 Erase가 들어갈 땐 반드시 Dry Run으로 검증 후 넣는다.

---

## 3. 정보 탐색

### Info("파일명")
- 설명: 종류(파일/폴더), 크기, 수정일을 보여준다.
- 예시: `Info("data.csv")`

### Tree("경로")
- 설명: 폴더 구조를 들여쓰기로 출력. 인자 생략 시 현재 폴더 기준.
- 예시: `Tree(".")` 또는 그냥 `Tree()`

### Size("경로")
- 설명: 파일 또는 폴더 전체 용량(MB, bytes)을 계산. 인자 생략 시 현재 폴더.
- 예시: `Size("./project")`

### Find("검색어","경로")
- 설명: 파일 이름에 검색어가 포함된 항목을 재귀적으로 찾는다. 경로 생략 시 현재 폴더부터.
- 예시: `Find("report",".")`
- 자주 틀리는 것: 아래 "file find" 공백형 명령어와 이름이 비슷해서 혼동한다. `Find("x")`는 괄호형(현재 폴더 기준 하위만 검색), `file find x`는 공백형(하위+상위 폴더까지 검색). 용도가 다르다.

### Search("검색어","경로")
- 설명: 파일 이름이 아니라 파일 "내용"에서 검색어를 찾는다. 5MB 넘는 파일과 바이너리 파일은 자동으로 건너뛴다.
- 예시: `Search("TODO","./src")`

### Replace("찾을내용","바꿀내용","경로")
- 설명: 대상 폴더 내 텍스트 파일에서 문자열을 일괄 치환한다.
- 예시: `Replace("http://","https://","./site")`
- 자주 틀리는 것: 되돌릴 수 없는 일괄 수정이므로 `?Replace(...)`로 대상 파일 목록을 먼저 확인해야 한다.

### Analyze("경로")
- 설명: 폴더 내 파일을 확장자별로 개수/용량 통계를 낸다.
- 예시: `Analyze("./downloads")`

### DuplicateFinder("경로")
- 설명: 내용이 완전히 동일한(해시 일치) 파일을 그룹으로 찾아준다. 이름이 달라도 내용이 같으면 잡힌다.
- 예시: `DuplicateFinder("./photos")`

### EmptyFinder("경로")
- 설명: 빈 폴더만 찾아 목록으로 보여준다(자동 삭제는 하지 않음).
- 예시: `EmptyFinder(".")`

### Exists("경로")
- 설명: 대상이 존재하는지만 확인한다. "존재함" / "존재하지 않음"으로 출력.
- 예시: `Exists("config.json")`
- 자주 틀리는 것: 이 명령은 삭제/생성 전에 미리 확인하는 용도이지, 존재 여부에 따라 자동으로 분기하지는 않는다. 조건 분기가 필요하면 jat코드 변수와 Autowork의 if를 함께 써야 한다.

---

## 4. 자동화 정리(Clean)/압축

### Clean("확장자","경로")
- 설명: 특정 확장자 파일을 모두 찾아 휴지통으로 이동(Lv.1과 동일한 안전 삭제).
- 예시: `Clean("tmp",".")`
- 자주 틀리는 것: 확장자 앞에 점(.)을 붙여도 되고 안 붙여도 된다(`"tmp"`와 `".tmp"` 둘 다 동작). 하지만 `"*.tmp"`처럼 와일드카드를 넣으면 안 된다. 매칭은 정확한 확장자 문자열로만 한다.

### Compress("폴더","파일.zip")
- 설명: 폴더를 zip으로 묶는다. 무압축(저장) 방식만 지원한다.
- 예시: `Compress("./project","project.zip")`
- 자주 틀리는 것: 압축률을 기대하면 안 된다. 용량 절감이 아니라 "여러 파일을 하나로 묶는" 용도로만 써야 한다.

### Extract("파일.zip","폴더")
- 설명: zip을 해제한다. 단, 무압축(저장) 방식 항목만 해제 가능하고, 일반 압축(Deflate) 방식으로 만들어진 zip의 해당 항목은 건너뛴다.
- 예시: `Extract("project.zip","./out")`
- 자주 틀리는 것: 다른 프로그램(7-Zip, Windows 탐색기 등)으로 만든 일반 zip 파일을 넣으면 "미지원" 개수가 많이 뜰 수 있다. jat의 Compress로 만든 zip을 다시 Extract할 때 가장 안전하다.

---

## 5. 자동화 - 예약/반복/조합

### Watch("폴더경로","실행할명령어")
- 설명: 폴더를 1초 간격으로 폴링하여 변경이 감지되면 지정 명령을 실행한다.
- 예시: `Watch("./drop","Compress(\"./drop\",\"out.zip\")")`
- 자주 틀리는 것: 두 번째 인자는 문자열 하나로 넘겨야 하므로, 그 안에 등장하는 큰따옴표는 반드시 백슬래시로 이스케이프(`\"`)해야 한다. 이걸 빠뜨리면 파싱 에러가 난다.

### Schedule("시간","실행할명령어")
- 설명: 지정 시간 뒤 한 번 실행. 시간 형식은 `숫자+s(초)/m(분)/h(시간)`.
- 예시: `Schedule("30s","Optimize()")`
- 자주 틀리는 것: 단위 문자(s/m/h)를 빠뜨리면 초 단위 숫자로 처리되긴 하지만, 의도를 명확히 하려면 항상 단위를 붙이는 게 안전하다.

### Macro("이름",[명령어1,명령어2,...])
- 설명: 명령어 여러 개를 이름으로 저장해둔다. 이후 `이름()`으로 호출하면 저장된 순서대로 실행된다.
- 예시: `Macro("cleanup",[Clean("tmp","."),Optimize()])` 다음 `cleanup()`
- 자주 틀리는 것: 대괄호 안 각 명령어는 따옴표로 감싸지 않는다. `Macro("x",["Clean(\"tmp\",\".\")"])` 처럼 문자열로 이중으로 감싸면 안 되고, `Macro("x",[Clean("tmp",".")])`처럼 명령어 그대로 나열한다.

### Batch([명령어1,명령어2,...])
- 설명: Macro와 문법은 같지만 저장하지 않고 그 자리에서 한 번만 순차 실행한다.
- 예시: `Batch([Create("a","1","txt"),Create("b","2","txt")])`

### Alias("이름","명령어")
- 설명: 명령어 하나를 짧은 이름으로 등록한다. 내부적으로 Macro와 동일한 저장소를 쓴다(단계가 1개인 매크로).
- 예시: `Alias("bk","Snapshot(\".\")")` 다음 `bk()`
- 자주 틀리는 것: 두 번째 인자 안의 따옴표도 Watch와 마찬가지로 이스케이프가 필요하다.

---

## 6. 시스템/프로세스

### Status()
- 설명: CPU 사용률, RAM 사용량, 디스크 여유공간을 한 번에 출력.
- 예시: `Status()`

### Monitor()
- 설명: 0.5초 간격으로 5회 샘플링하여 CPU/디스크 상태를 연속 출력.
- 예시: `Monitor()`

### Run("프로그램이름")
- 설명: 외부 프로그램을 실행한다(비동기, 결과를 기다리지 않음).
- 예시: `Run("notepad")` (Windows) / `Run("gedit")` (Linux)
- 자주 틀리는 것: 공백형 `run script.jat`와 이름이 같아 보이지만 완전히 다른 기능이다. 괄호가 있으면(`Run("x")`) 프로그램 실행, 공백만 있으면(`run x.jat`) jat코드 스크립트 실행이다.

### Kill("프로그램이름")
- 설명: 이름이 일치하는 프로세스를 강제 종료. Windows는 `.exe`를 자동으로 붙여 비교한다.
- 예시: `Kill("notepad")`

### Service("이름","start|stop|restart|status")
- 설명: 시스템 서비스를 제어한다(Windows: 서비스 관리자, Linux: systemctl). 두 번째 인자 생략 시 status로 동작.
- 예시: `Service("nginx","restart")`
- 자주 틀리는 것: 관리자 권한/sudo 권한이 없으면 실패한다. 에러 메시지에 권한 문제가 명시되니 확인할 것.

### Startup()
- 설명: 시작 프로그램 목록 조회(Windows: 레지스트리 Run 키, Linux: `~/.config/autostart`).
- 예시: `Startup()`

### Doctor()
- 설명: 디스크 여유공간, CPU 사용률을 점검해 정상/경고로 표시.
- 예시: `Doctor()`

### Repair()
- 설명: Windows는 `sfc /scannow`를 관리자 권한으로 실행. Linux는 최근 시스템 오류 로그(journalctl)를 보여준다(자동 복구는 하지 않음).
- 예시: `Repair()`

### Optimize()
- 설명: 임시파일 폴더 정리 + 휴지통 비우기.
- 예시: `Optimize()`
- 자주 틀리는 것: 휴지통을 비우면 Delete(Lv.1)로 옮겨둔 파일도 함께 사라진다. Remove(Lv.2)로 격리해둔 `.jat_trash` 폴더는 영향받지 않는다.

### Benchmark()
- 설명: 임시 파일 50MB를 써보고 읽어보며 디스크 속도(MB/s)를 측정.
- 예시: `Benchmark()`

---

## 7. 네트워크

### Download("url","저장할파일명")
- 설명: URL에서 파일을 받아 저장(내부적으로 curl 사용).
- 예시: `Download("https://example.com/a.zip","a.zip")`
- 자주 틀리는 것: curl이 시스템에 설치되어 있지 않으면 실패한다.

### Upload("파일","서버URL")
- 설명: 파일을 HTTP PUT으로 업로드(curl 사용).
- 예시: `Upload("report.pdf","https://myserver.com/upload/report.pdf")`

### Ping("주소")
- 설명: 시스템 ping 명령을 그대로 호출해 결과를 보여준다.
- 예시: `Ping("8.8.8.8")`

### Network()
- 설명: 네트워크 어댑터별 연결 상태를 보여준다.
- 예시: `Network()`

### IP()
- 설명: 현재 활성 IP 주소(어댑터별)를 보여준다.
- 예시: `IP()`

### DNS("도메인")
- 설명: 도메인의 IPv4/IPv6 주소를 조회.
- 예시: `DNS("github.com")`

### Port()
- 설명: 현재 열려서 LISTEN 중인 TCP 포트 목록을 보여준다.
- 예시: `Port()`

### API("url")
- 설명: GET 요청 결과를 그대로 출력(curl 사용, 3000자 넘으면 잘라서 표시).
- 예시: `API("https://api.github.com")`

---

## 8. 보안

### Checksum("파일")
- 설명: SHA-256 해시값을 계산해 출력.
- 예시: `Checksum("installer.exe")`

### HashCompare("파일1","파일2")
- 설명: 두 파일의 SHA-256이 같은지 비교.
- 예시: `HashCompare("a.zip","b.zip")`

### Encrypt("파일","비밀번호")
- 설명: SHA-256 기반 스트림 암호로 파일을 암호화한다. `.enc` 파일이 새로 생긴다.
- 예시: `Encrypt("secret.txt","mypassword")`
- 자주 틀리는 것: 이건 AES가 아니다. 표준 암호화 규격이 필요한 민감한 용도(법적/보안 규정이 있는 데이터)에는 쓰면 안 된다. 개인적인 가벼운 보호 용도로만 쓴다.

### Decrypt("파일","비밀번호")
- 설명: Encrypt로 만든 파일을 원래대로 복호화.
- 예시: `Decrypt("secret.txt.enc","mypassword")`
- 자주 틀리는 것: 비밀번호가 틀려도 에러 없이 깨진 파일이 그대로 생성된다. 복호화 후 파일 내용을 반드시 확인해야 한다.

---

## 9. 강력한 도구

### Diff("파일1","파일2")
- 설명: 두 텍스트 파일을 줄 단위로 비교해 다른 줄을 보여준다.
- 예시: `Diff("v1.txt","v2.txt")`

### Snapshot("폴더")
- 설명: 현재 작업 폴더의 `.jat_snapshots`에 타임스탬프를 붙여 전체 복사본을 만든다.
- 예시: `Snapshot("./project")`

### Backup("파일/폴더")
- 설명: Snapshot과 동일하지만 저장 위치가 `.jat_backup`이다. 단일 파일 백업에 더 적합한 이름으로 구분해둔 것.
- 예시: `Backup("config.json")`

### Lock("파일") / Unlock("파일")
- 설명: 읽기전용 속성을 켜고 끈다(Windows: 파일 속성, Linux: 쓰기 권한 제거/부여).
- 예시: `Lock("final.docx")` / `Unlock("final.docx")`

### Holder("파일")
- 설명: 현재 그 파일을 점유(사용) 중인 프로세스가 있는지 확인.
- 예시: `Holder("database.db")`
- 자주 틀리는 것: Windows에서는 "사용 중"이라는 사실만 알려주고 어떤 프로세스인지는 특정하지 못한다(제한사항). 프로세스명까지 정확히 나오는 건 Linux뿐이다.

### Risk("경로")
- 설명: 하위 폴더 수, 파일 수, 사용중 여부를 종합해 위험 점수를 매겨 낮음/중간/높음으로 보여준다. 삭제 계열 명령 실행 전 참고용.
- 예시: `Risk("./project")`

### Estimate("경로")
- 설명: 파일 개수와 총 용량을 바탕으로 처리 예상 시간을 계산(초당 80MB 가정).
- 예시: `Estimate("./project")`

### Explain("명령어","할 말")
- 설명: 특정 명령어를 이후 호출할 때마다 등록해둔 설명 문구를 먼저 보여주게 만든다. 팀 내 사용 규칙을 남기는 용도.
- 예시: `Explain("Erase","이 명령은 되돌릴 수 없습니다. 꼭 Risk로 먼저 확인!")`
- 자주 틀리는 것: 등록만 하고 실제로 동작을 막지는 않는다. 어디까지나 안내 문구이지 실행 차단 기능이 아니다.

---

## 10. 편의 명령어

### Cd("경로")
- 설명: 현재 작업 폴더를 변경한다. 이후 상대경로 명령은 모두 이 위치 기준으로 동작.
- 예시: `Cd("./project")`

### Path()
- 설명: 현재 작업 폴더 경로를 출력.
- 예시: `Path()`

### Timestamp()
- 설명: 현재 날짜/시각을 `YYYY-MM-DD HH:MM:SS` 형식으로 출력.
- 예시: `Timestamp()`

### Env("변수명")
- 설명: 환경변수 값을 조회.
- 예시: `Env("PATH")`

### Random("최소","최대")
- 설명: 지정 범위 내 정수 난수 생성.
- 예시: `Random("1","100")`

### Count("확장자","경로")
- 설명: 특정 확장자 파일 개수만 센다(Clean과 달리 삭제하지 않음).
- 예시: `Count("log","./var")`

### Sort("경로","name|size|date")
- 설명: 폴더 항목을 이름/크기/날짜 기준으로 정렬해 목록으로 보여준다. 세 번째 인자 생략 시 이름순.
- 예시: `Sort(".","size")`

### Merge("파일1","파일2","결과파일")
- 설명: 두 파일을 순서대로 이어붙여 새 파일로 저장.
- 예시: `Merge("part1.txt","part2.txt","full.txt")`

### WordCount("파일")
- 설명: 줄 수/단어 수/글자 수를 계산.
- 예시: `WordCount("report.txt")`

---

## 11. 파일탐색 (공백형 명령어, 괄호 없음)

### file find <이름>
- 설명: 현재 폴더 하위 전체를 재귀 검색하고, 상위 폴더들도 1단계씩 함께 확인한다.
- 예시: `file find report`
- 자주 틀리는 것: 괄호형이 아니므로 `file find("report")`처럼 쓰면 동작하지 않는다. 그냥 공백으로 이어 쓴다. 이름에 공백이 포함된 경우는 지원하지 않는다(공백 기준으로 잘라 인식하기 때문).

### file ext find <확장자>
- 설명: 특정 확장자 파일만 필터링해 같은 방식으로 검색.
- 예시: `file ext find pdf`
- 자주 틀리는 것: 확장자 앞에 점을 붙여도 안 붙여도 동작하지만, `file ext find *.pdf`처럼 와일드카드를 쓰면 안 된다.

---

## 12. 자동입력 (마우스/키보드)

### Wait(ms)
- 설명: 지정 밀리초만큼 대기.
- 예시: `Wait(1000)` (1초 대기)

### MouseMoveX(n) / MouseMoveY(n)
- 설명: 현재 커서 위치 기준 상대 이동. X, Y는 별도 명령이다.
- 예시: `MouseMoveX(100)` 다음 `MouseMoveY(-50)`

### CenterMouse()
- 설명: 커서를 화면 정중앙으로 이동. 실패해도 에러 없이 조용히 넘어간다.
- 예시: `CenterMouse()`

### MouseLeft() / MouseRight()
- 설명: 현재 커서 위치에서 좌클릭 / 우클릭.
- 예시: `MouseLeft()`
- 자주 틀리는 것: `Mouse("Click")`, `Mouse("Right")`도 남아있지만 이는 하위 호환용이다. 새로 작성할 땐 `MouseLeft()` / `MouseRight()`를 쓴다.

### Keyboard("key")
- 설명: 지정 키를 누르고 뗀다. `enter`, `tab`, `esc`/`escape`, `space`, `backspace`는 이름으로 지정 가능하고, 그 외에는 알파벳/숫자 한 글자만 지원.
- 예시: `Keyboard("enter")` / `Keyboard("a")`
- 자주 틀리는 것: 두 글자 이상의 문자열(`"abc"`)은 지원하지 않는다. 한 글자씩 나눠 여러 번 호출해야 한다. Linux는 X11 세션(DISPLAY)이 없으면 실패한다.

---

## 13. 웹 검색

### Jweb("검색어")(옵션)
- 설명: Google과 Bing 검색 결과 페이지를 기본 브라우저로 연다. 괄호가 두 번 붙는 특수 문법이다.
- 옵션: video(영상), image(이미지), news(뉴스), read(전체, 기본값), code(코드 관련 검색어 보정), doc(문서 관련 검색어 보정)
- 예시: `Jweb("c++ std::filesystem")(doc)`
- 옵션 없이 쓰기: `Jweb("오늘 날씨")()`  또는 옵션 괄호를 아예 생략 가능
- 자주 틀리는 것: 첫 번째 괄호와 두 번째 괄호 사이에 다른 문자를 넣으면 안 된다. `Jweb("검색어") (옵션)`처럼 공백이 있어도 동작은 하지만, 습관적으로 붙여쓰는 게 안전하다. 검색 결과를 터미널 안에서 텍스트로 보여주는 기능이 아니라 브라우저를 여는 기능이라는 점도 자주 오해하는 부분이다.

---

## 14. 자동화 블록

### Autowork( 명령어들... if(조건){...} else {...} )
- 설명: 여러 명령을 한 블록 안에 줄바꿈으로 나열해 순차 실행한다. `if(조건){...}else{...}` 조건 분기도 지원한다. 조건식은 jat코드 변수를 사용한 비교식(`==`,`!=`,`<`,`>`,`<=`,`>=`)이다.
- 예시:
```
Autowork(
    a = 10
    Console(a)
    if(a > 5)
    {
        Create("big","x","txt")
    }
    else
    {
        Create("small","x","txt")
    }
)
```
- 여러 줄 입력 방법: 터미널에 `Autowork(` 까지만 입력하고 엔터를 치면 괄호가 닫힐 때까지 `...>` 프롬프트로 계속 다음 줄을 받는다. 마지막 줄에 닫는 괄호 `)`를 입력하면 그때 전체가 한 번에 실행된다.
- 자주 틀리는 것: 블록 안의 개별 명령 사이에는 콤마를 넣지 않는다(Batch/Macro와 다름). 한 줄에 한 명령씩 그냥 나열한다. else 블록은 선택사항이라 생략 가능하지만, 생략하면 조건이 거짓일 때 아무 일도 일어나지 않는다.

---

## 15. 스크립팅 (jat코드)

### Console(값)
- 설명: 값이나 계산식 결과를 출력한다.
- 예시: `Console("hello")` / `Console(1)` / `Console(a+5)`

### 변수 = 값
- 설명: 변수를 선언하고 즉시 대입. 타입 선언이 없고, 같은 변수에 숫자와 문자열을 번갈아 대입할 수 있다.
- 예시: `a = 15` 다음 `a = "hello"` (둘 다 정상 동작)

### list 변수 = ["문자열1","문자열2"]
- 설명: 문자열로만 이루어진 리스트를 만든다.
- 예시: `list fruits = ["apple","banana"]`
- 자주 틀리는 것: 리스트 항목은 반드시 따옴표로 감싼 문자열이어야 한다. `list a = [1,2,3]`처럼 숫자를 그냥 넣으면 파싱 에러가 난다.

### 사칙연산 (+, -, *, /)
- 설명: 숫자끼리는 계산, 문자열이 하나라도 섞이면 `+`는 이어붙이기로 동작한다.
- 예시: `result = a + 5` / `msg = "값: " + a`
- 자주 틀리는 것: 문자열에 `-`, `*`, `/`를 쓰면 에러가 난다. 이 세 연산자는 숫자끼리만 허용된다.

### 비교연산 (==, !=, <, >, <=, >=)
- 설명: Autowork의 if 조건에서만 쓰인다. 결과는 참/거짓이며 내부적으로 1/0으로 처리된다.
- 예시: `if(a == 10) { Console("맞음") }`

### 주석 (//)
- 설명: `//` 뒤 내용은 실행에서 무시된다. 줄 단위로만 적용된다(블록 주석 없음).
- 예시: `a = 5 // 초기값 설정`

### run script.jat
- 설명: `.jat` 스크립트 파일을 처음부터 끝까지 한 줄씩 읽어 실행한다. 오류가 난 줄이 있어도 나머지 줄은 계속 실행되며, 마지막에 오류 개수를 알려준다.
- 예시: `run mytest.jat`
- 자주 틀리는 것: 확장자가 `.jat`가 아니어도 실행 자체는 되지만(파일 내용만 읽으므로), 관례상 `.jat`로 통일해서 관리하는 게 좋다. 스크립트 안에서 변수는 세션 전체에서 공유되므로, 터미널에서 인라인으로 쓰던 변수 이름과 스크립트 안 변수 이름이 겹치면 값이 덮어써질 수 있다.

---

## 16. Lua 스크립트 실행 - Hi 출력 상세 튜토리얼

Lua 실행은 jat 자체 문법이 아니라, 시스템에 설치된 `lua` 인터프리터를 별도 프로세스로 실행하고 그 출력을 jat이 받아오는 방식이다. 이 점을 이해하지 못하면 실수가 잦다.

### 1단계: lua 인터프리터가 설치되어 있는지 먼저 확인
jat 자체에는 Lua 실행기가 내장되어 있지 않다. 시스템 PATH에 `lua`(Windows는 `lua.exe`) 명령이 있어야 한다. 설치가 안 되어 있으면 아무리 문법을 맞게 써도 항상 실패한다.

### 2단계: Hi를 출력하는 .lua 파일을 jat으로 직접 생성
Lua 소스 자체는 jat의 Create로 만들 수 있다.
```
Create("hi","print(\"Hi\")","lua")
```
이 명령은 `hi.lua`라는 파일을 만들고 그 안에 `print("Hi")` 한 줄을 써넣는다. 여기서 가장 많이 틀리는 부분: Create의 두 번째 인자(내용)는 jat 명령어 문자열 규칙을 따르므로, Lua 코드 안에 있는 큰따옴표(`print("Hi")`의 따옴표)는 반드시 백슬래시로 이스케이프해야 한다. `\"Hi\"`로 써야 실제 파일에는 `"Hi"`로 정확히 저장된다. 이스케이프를 빠뜨리면 jat 명령어 자체가 인자 개수를 잘못 세어 에러가 난다.

### 3단계: 생성된 파일 확인(선택이지만 실무에서는 권장)
```
Info("hi.lua")
```
파일이 실제로 만들어졌는지, 크기가 0바이트가 아닌지 먼저 확인한다.

### 4단계: 실행
```
Lua("hi.lua","")
```
두 번째 인자는 Lua 스크립트에 넘길 실행 인자 문자열이다. Hi 출력 예제처럼 인자가 필요 없으면 빈 문자열 `""`을 그대로 써준다. 인자를 아예 생략하는 것도 가능하다(`Lua("hi.lua")`), 이 경우 인자 없이 실행된다.

### 5단계: 결과 읽는 법
실행하면 다음과 같은 형식으로 출력된다.
```
=== Lua 실행결과 (exitCode=0) ===
Hi
```
`exitCode=0`은 정상 종료를 의미한다. 0이 아니면 Lua 스크립트 내부에서 오류가 났거나 비정상 종료된 것이다. 출력 내용에는 Lua의 `print` 결과와 오류 메시지(stderr)가 함께 섞여서 나온다(둘 다 캡처하기 때문).

### 자주 틀리는 것 모음 (Lua 관련)
- lua 인터프리터 미설치: exitCode=-1과 함께 "lua 명령어를 찾을 수 없습니다" 메시지가 뜬다. 문법 문제가 아니라 설치 문제이므로 코드를 고쳐도 소용없다.
- 큰따옴표 이스케이프 누락: Create로 .lua 파일을 만들 때 Lua 코드 안의 `"`를 그대로 두면(`\"` 대신 `"`만 쓰면) jat 명령어 인자 파싱이 그 지점에서 문자열을 끝난 것으로 착각해 이후 내용이 깨지거나 에러가 난다.
- 인자 전달 형식 착각: 두 번째 인자(`"인자1 인자2"`)는 공백으로 구분된 하나의 문자열이다. 배열이나 여러 개의 별도 인자로 나눠 쓰지 않는다.
- 30초 제한: 스크립트가 30초 안에 끝나지 않으면 강제 종료되고 exitCode=-1과 타임아웃 메시지가 출력된다. 무한 루프가 있는 스크립트를 테스트할 때 이 제한을 잊고 "왜 결과가 안 나오지"라며 오래 기다리는 경우가 있다.
- 실행 경로 착각: `Lua("hi.lua",...)`의 첫 번째 인자는 현재 작업 폴더(`Path()`로 확인 가능) 기준 상대경로다. Cd로 폴더를 옮긴 뒤에 이전 폴더 기준 경로를 그대로 쓰면 "스크립트를 찾을 수 없습니다" 에러가 난다.

---

## 17. 터미널 UX

### help()
- 설명: 전체 명령어 요약 목록을 출력.

### version()
- 설명: 현재 버전 정보 출력.

### clear()
- 설명: 화면을 지운다(OS의 cls/clear 호출).

### history()
- 설명: 지금까지 입력한 명령 기록을 번호와 함께 출력.

---

## 공통으로 가장 자주 틀리는 것 Top 8

1. 문자열 인자에 따옴표를 빼먹는다 - `Create(a,1,txt)` (X) → `Create("a","1","txt")` (O)
2. `Dry Run(?)`과 `Silent($)`를 명령어 이름 뒤에 붙인다 - `Create?(...)` (X) → `?Create(...)` (O)
3. `Find`(괄호형, 하위만)와 `file find`(공백형, 상하위 모두)를 혼동한다.
4. `Run("프로그램")`(괄호형, 외부 프로그램 실행)과 `run script.jat`(공백형, jat코드 스크립트 실행)을 혼동한다.
5. `Macro`/`Batch`의 대괄호 안 명령어를 문자열로 한 번 더 감싼다 - 감싸면 안 되고 명령어를 그대로 나열한다.
6. `Watch`/`Alias`처럼 두 번째 인자 안에 다른 jat 명령어 문자열을 넣을 때, 그 안의 큰따옴표를 이스케이프하지 않는다.
7. Autowork 블록 안에서 명령어 사이에 콤마를 넣는다 - Autowork는 줄바꿈으로만 구분한다.
8. Erase/Replace처럼 되돌릴 수 없는 명령을 Dry Run 없이 바로 실행한다.
