# jat Terminal v7 명령어 실무 가이드

## 파일 관리

### Create (자주 사용)
- 왜 쓰는가: 파일 생성과 내용 작성을 한 번에 처리
- 예시: `Create("note","hello world","txt")`
- 효율 팁: 리다이렉션 연산자 없이 이름/내용/확장자를 인자로 바로 지정

### Delete (자주 사용)
- 왜 쓰는가: 즉시 삭제가 아닌 휴지통 이동(Lv.1)으로 실수 복구 여지 확보
- 예시: `Delete("temp.log")`
- 효율 팁: `Remove-Item`과 달리 기본 동작 자체가 복구 가능한 삭제

### Copy
- 왜 쓰는가: 파일/폴더 복사를 진행률 표시와 함께 수행
- 예시: `Copy("data.csv","backup/data.csv")`
- 효율 팁: `Copy-Item -Recurse` 없이 폴더 여부 자동 판별

### Move
- 왜 쓰는가: 이름 변경 없이 위치만 이동
- 예시: `Move("draft.md","archive/draft.md")`
- 효율 팁: 드라이브 간 이동 실패 시 복사+삭제로 자동 대체

### Rename
- 왜 쓰는가: 같은 폴더 내 이름만 변경할 때 경로 재입력 불필요
- 예시: `Rename("v1.txt","v2.txt")`
- 효율 팁: 전체 경로 대신 새 이름만 전달

### Duplicate
- 왜 쓰는가: 원본을 유지한 채 사본 생성
- 예시: `Duplicate("config.json","config.backup.json")`
- 효율 팁: Copy와 동일 동작이나 의도가 "백업 목적"임을 코드에서 명시

## 자동화

### Autowork (자주 사용)
- 왜 쓰는가: 여러 명령을 조건 분기와 함께 한 블록에서 순차 실행
- 예시:
```
Autowork(
    Create("log","start","txt")
    if(a > 5)
    {
        Delete("temp.txt")
    }
    else
    {
        Create("keep","x","txt")
    }
)
```
- 효율 팁: 별도 스크립트 파일 없이 터미널 안에서 조건부 배치 작업 작성

### Lua (자주 사용)
- 왜 쓰는가: 프로세스 격리 상태로 Lua 스크립트를 실행하고 결과 회수
- 예시: `Lua("script.lua","arg1 arg2")`
- 효율 팁: 별도 런처 스크립트 없이 인자 전달과 30초 타임아웃 처리 내장

### Watch
- 왜 쓰는가: 폴더 변경을 감지해 지정 명령을 자동 실행
- 예시: `Watch("./drop","Compress(\"./drop\",\"out.zip\")")`
- 효율 팁: 파일 감시 데몬 설정 없이 1줄로 감시-실행 연결

### Schedule
- 왜 쓰는가: 지정 시간 뒤 명령을 1회 예약 실행
- 예시: `Schedule("10m","Optimize()")`
- 효율 팁: 작업 스케줄러 등록 없이 세션 내에서 즉시 예약

### Macro
- 왜 쓰는가: 반복 작업 순서를 이름으로 저장해 재사용
- 예시: `Macro("cleanup",[Clean("tmp","."),Optimize()])`
- 효율 팁: 함수 정의 없이 배열 하나로 재사용 가능한 명령 묶음 생성

### Batch
- 왜 쓰는가: 저장 없이 이번 한 번만 명령 여러 개를 순서대로 실행
- 예시: `Batch([Create("a","1","txt"),Create("b","2","txt")])`
- 효율 팁: 세미콜론 체이닝 없이 배열 하나로 순차 실행

### Alias
- 왜 쓰는가: 긴 명령을 짧은 이름으로 매핑
- 예시: `Alias("bk","Snapshot(\".\")")`
- 효율 팁: 프로필 스크립트 편집 없이 세션 중 즉시 별칭 등록

## 시스템 제어

### CenterMouse (자주 사용)
- 왜 쓰는가: 커서를 화면 중앙으로 고정해 자동화 시작 지점을 통일
- 예시: `CenterMouse()`
- 효율 팁: 좌표 계산 없이 호출 한 번으로 중앙 이동

### MouseLeft
- 왜 쓰는가: 좌클릭만 별도로 트리거해 우클릭과 로직 분리
- 예시: `MouseLeft()`
- 효율 팁: 버튼 종류를 옵션값으로 넘기지 않고 함수명으로 바로 구분

### MouseRight
- 왜 쓰는가: 우클릭 컨텍스트 메뉴 자동화에 사용
- 예시: `MouseRight()`
- 효율 팁: 이벤트 종류 문자열 비교 없이 전용 명령으로 오작동 방지

## 웹/네트워크

### Jweb (자주 사용)
- 왜 쓰는가: 검색어와 검색 유형(영상/이미지/뉴스 등)을 한 번에 지정해 결과 오픈
- 예시: `Jweb("c++ std::filesystem")(doc)`
- 효율 팁: URL 조립 없이 옵션 하나로 검색 카테고리 전환

### Download
- 왜 쓰는가: URL에서 파일을 지정 이름으로 즉시 저장
- 예시: `Download("https://example.com/file.zip","file.zip")`
- 효율 팁: `Invoke-WebRequest` 대비 진행 표시가 붙은 짧은 호출

### Ping
- 왜 쓰는가: 대상 서버 응답 여부를 빠르게 확인
- 예시: `Ping("8.8.8.8")`
- 효율 팁: 옵션 플래그 없이 기본값으로 즉시 확인

### API
- 왜 쓰는가: GET 요청 결과를 터미널에서 바로 확인
- 예시: `API("https://api.github.com")`
- 효율 팁: 헤더/파싱 설정 없이 응답 원문을 바로 출력

## 안전/유틸리티

### ? (Dry Run) (자주 사용)
- 왜 쓰는가: 실제 실행 전 결과를 미리 확인해 삭제/이동 충돌 방지
- 예시: `?Erase("build")`
- 효율 팁: `-WhatIf` 옵션을 명령마다 붙일 필요 없이 접두어 하나로 통일

### $ (Silent) (자주 사용)
- 왜 쓰는가: 매크로/배치 내부에서 입력 에코 없이 조용히 실행
- 예시: `$Delete("cache.tmp")`
- 효율 팁: 출력 억제 파이프(`| Out-Null`) 없이 접두어로 처리

### Exists
- 왜 쓰는가: 다음 명령 실행 전 대상 존재 여부를 먼저 확인
- 예시: `Exists("config.json")`
- 효율 팁: `Test-Path`와 동일 목적을 인자 하나로 처리

### Timestamp
- 왜 쓰는가: 로그/백업 파일명에 쓸 현재 시각을 즉시 확인
- 예시: `Timestamp()`
- 효율 팁: 날짜 포맷 문자열 지정 없이 고정 포맷으로 즉시 출력

### Env
- 왜 쓰는가: 스크립트 조건 분기에 쓸 환경변수 값을 조회
- 예시: `Env("PATH")`
- 효율 팁: `$env:` 접두어 없이 함수 호출 하나로 조회

## 스크립팅

### Console
- 왜 쓰는가: 값이나 변수 계산 결과를 화면에 출력해 확인
- 예시: `Console("hello")` / `Console(a+5)`
- 효율 팁: 문자열/숫자 구분 없이 동일 함수로 출력

### 변수 선언
- 왜 쓰는가: 세션 내내 유지되는 값을 저장해 반복 계산 회피
- 예시: `a = 15` / `a = "hello"`
- 효율 팁: 타입 선언 없이 재할당 시 타입 자동 전환

### list
- 왜 쓰는가: 문자열 여러 개를 한 변수로 묶어 관리
- 예시: `list names = ["hello","so"]`
- 효율 팁: 배열 선언 문법 없이 대괄호 나열만으로 생성

### run
- 왜 쓰는가: 작성해둔 .jat 스크립트를 파일 단위로 실행
- 예시: `run script.jat`
- 효율 팁: 인터프리터 경로 지정 없이 파일명만으로 실행

### 주석(//)
- 왜 쓰는가: 스크립트 내 의도를 남기되 실행에 영향 없음
- 예시: `// 임시 파일 정리 구간`
- 효율 팁: 블록 주석 문법 없이 줄 단위로 즉시 비활성화

## 자주 쓰는 명령어 Top 10

| 순위 | 명령어 | 카테고리 | 핵심 용도 |
|---|---|---|---|
| 1 | Create | 파일 관리 | 파일 생성+내용 작성 동시 처리 |
| 2 | Delete | 파일 관리 | 안전한 임시 삭제(휴지통 이동) |
| 3 | ? (Dry Run) | 안전/유틸리티 | 실행 전 결과 미리보기 |
| 4 | $ (Silent) | 안전/유틸리티 | 매크로 내부 무음 실행 |
| 5 | Autowork | 자동화 | 조건부 다중 명령 실행 |
| 6 | Copy | 파일 관리 | 파일/폴더 복사 |
| 7 | Jweb | 웹/네트워크 | 옵션별 웹 검색 |
| 8 | CenterMouse | 시스템 제어 | 자동화 시작 좌표 고정 |
| 9 | Lua | 자동화 | 격리된 Lua 스크립트 실행 |
| 10 | Console | 스크립팅 | 값/변수 출력 확인 |
